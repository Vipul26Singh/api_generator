package {{ package }}.entity;

import jakarta.persistence.*;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;
{% for imp in imports %}
import {{ imp }};
{% endfor %}

/**
 * JPA entity for the {@code {{ table_name }}} table.
 *
 * <p>This class is auto-generated by microservice-generator.
 * Add custom logic via {@code {{ class_name }}Hooks} rather than editing here,
 * so that regeneration does not overwrite your changes.</p>
 */
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
@Entity
@Table(name = "{{ table_name }}")
public class {{ class_name }} {

{% for col in columns %}
{% if col.is_pk %}
    /** Primary key — auto-incremented by the database. */
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
{% elif col.in_service_fk %}
    /** Many-to-one relationship to {@link {{ col.java_type }}}. */
{% elif col.cross_service_fk_comment %}
    {{ col.cross_service_fk_comment }}
{% endif %}
{% for ann in col.annotations %}
    {{ ann }}
{% endfor %}
    private {{ col.java_type }} {{ col.field_name }};

{% endfor %}
}
